# Figured I'd build myself a website

Uses the same technology as the ACCESS-NRI Interactive Intake Catalog Viewer. This is basically just a Vue app, built with Vite, that gets served statically via github pages.

Just a sandbox for some random ideas.
